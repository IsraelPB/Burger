<div class="container-fluid">
    <table class="table table-borderless">
        {{!-- <div class="row" id="row1"> --}}
            <div class="col-sm-12">
                <div class="container">
                    <img src="img/burger-1487481__340.jpg" class="center" alt="...">
                    <h1 class="display-4">Burgers!Burgers Everywhere!</h1>
                </div>
            </div>
            {{!-- </div> --}}
        <div class="row">
            <div class="col-sm-4">
                <h2>Add a Burger</h2>
                <form id="addburger" class="button-size">
                    <div class="form-group">
                        <input type="text" id="burger" name="burger_name">
                    </div>
                    <div>
                        <button id="addburgerbtn" type="submit">Add Burger</button>
                    </div>
                </form>
            </div>
            <div class="col-sm-4">
                <h2>Menu</h2>
                <ul>
                    {{#each burgers}}
                    {{#unless this.devoured}}
                    <li>
                        {{!-- <form action="/api/burgers" method="POST"> --}}
                            <p>{{this.burger_name}}<p>
                                <button id="devourit" type="submit" class='burgerAvailable' data-id="{{this.id}}">Eat it!</button>
                        {{!-- </form> --}}
                    </li>
                    {{/unless}}
                    {{/each}}
                </ul>
            </div>

            <div class="col-sm-4">
                <h2>Eaten</h2>
                <ul>
                    {{#each burgers}}
                    {{#if this.devoured}}
                    
                    <div class='burgerEaten'>
                        <li>
                           
                                <div>
                                    <p>{{this.burger_name}}</p>
                                   {{!-- <button id="deleteBurger" type="submit" class='burgerAvailable' data-id="{{this.id}}">Eat it!</button> --}}
                                </div>
                            {{!-- </form> --}}
                        </li>
                    </div>
                    {{/if}}
                    {{/each}}
                </ul>
            </div>
        </div>
    </table>
</div>